#!/usr/bin/expect -f

set timeout -1
spawn docker exec -ti $env(CYBER_DOCKER) cyber tx slashing unjail --from=$env(CYBER_KEY_NAME) --chain-id $env(CYBER_CHAIN_ID)
expect "* passphrase:"  {send -- "$env(CYBER_PASS)\r"}
expect "* broadcasting *" {send -- "y\r"}

expect eof